# Examples

Generic example artifacts used by later phases live here.

- `payloads/`: sample message payloads and other static test assets
- `containers/`: example container contexts used by Lambda-image and Fargate integration tests

Keep these artifacts generic. They should prove platform behavior without encoding workflow-specific scientific logic.
