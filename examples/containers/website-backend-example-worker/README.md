# website-backend-example-worker

This directory is reserved for the generic example worker container context used by later Terraform integration tests.

Keep the image behavior generic and focused on proving platform plumbing rather than workflow-specific logic.
