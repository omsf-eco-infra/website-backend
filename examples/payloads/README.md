# Example payloads

Example payloads use snake_case family names plus dot-delimited scenarios.

Examples:

- `task_message.matching.json`
- `orchestration_message.add_tasks.valid.json`

Payloads in this directory should stay generic and reusable across Terraform and Python tests.
