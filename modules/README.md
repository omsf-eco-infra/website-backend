# Terraform modules

Reusable Terraform modules live in this directory.

- `container-image`
- `lambda-deploy`
- `orchestration`
- `task-queue`
- `fargate-compute`
- `web-interface`

Module directory names are part of the repo convention and should remain hyphenated to match the public module names used in `PLAN.md`.
