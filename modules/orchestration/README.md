# orchestration

This directory is reserved for the reusable orchestration Terraform module described in `PLAN.md`.

Use the canonical module name `orchestration` for module source paths, docs, and tests.
