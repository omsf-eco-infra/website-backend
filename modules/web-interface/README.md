# web-interface

This directory is reserved for the reusable web-interface Terraform module described in `PLAN.md`.

Use the canonical module name `web-interface` for module source paths, docs, and tests.
