# fargate-compute

This directory is reserved for the reusable Fargate compute Terraform module described in `PLAN.md`.

Use the canonical module name `fargate-compute` for module source paths, docs, and tests.
