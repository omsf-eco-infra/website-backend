name: Terraform

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: terraform-${{ github.ref }}
  cancel-in-progress: false

jobs:
  support-smoke:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Pixi
        uses: prefix-dev/setup-pixi@v0.9.3
        with:
          pixi-version: v0.63.0
          environments: dev
          cache: true
          cache-write: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}

      - name: Run local OpenTofu support smoke test
        run: |
          pixi run -e dev tofu -chdir=tests/tf/support/smoke init
          pixi run -e dev tofu -chdir=tests/tf/support/smoke test -test-directory=.

  ci-smoke:
    runs-on: ubuntu-latest
    needs: support-smoke
    if: >-
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository) ||
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')

    permissions:
      contents: read
      id-token: write

    env:
      TF_IN_AUTOMATION: "true"
      TF_VAR_owner: ${{ github.repository_owner }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Pixi
        uses: prefix-dev/setup-pixi@v0.9.3
        with:
          pixi-version: v0.63.0
          environments: dev
          cache: true
          cache-write: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v5.1.1
        with:
          role-to-assume: ${{ secrets.AWS_GHA_TEST_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}
          role-session-name: website-backend-ci-${{ github.run_id }}

      - name: Run real-AWS OpenTofu CI smoke test
        run: |
          pixi run -e dev tofu -chdir=tests/tf/ci-smoke init
          pixi run -e dev tofu -chdir=tests/tf/ci-smoke test -test-directory=.

      - name: Run real-AWS lambda-deploy module test
        run: |
          pixi run -e dev tofu -chdir=tests/tf/lambda-deploy init
          pixi run -e dev tofu -chdir=tests/tf/lambda-deploy test -test-directory=.

      - name: Run real-AWS orchestration module test
        run: |
          pixi run -e dev tofu -chdir=tests/tf/orchestration init
          pixi run -e dev tofu -chdir=tests/tf/orchestration test -test-directory=.

      - name: Run real-AWS fargate-compute module test
        run: |
          pixi run -e dev tofu -chdir=tests/tf/fargate-compute init
          pixi run -e dev tofu -chdir=tests/tf/fargate-compute test -test-directory=.
